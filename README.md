# Ganeshraj A — AI Engineer Portfolio

A clean, responsive portfolio for Ganeshraj A, focused on Generative AI, Agentic RAG, LLM engineering, Knowledge Graphs and Multi-Agent AI.

## Files

- `index.html` — main portfolio
- `style.css` — complete styling
- `script.js` — navigation and scroll animations
- `assets/images/education/` — place BITS Pilani and RMK logos here
- `assets/images/experience/` — place Cognizant logo here
- `assets/Ganeshraj_A_Resume.pdf` — place your resume here

## Before publishing

1. Replace the `#` LinkedIn links with your LinkedIn URL.
2. Replace the `#` GitHub links with your GitHub URL.
3. Put your resume at `assets/Ganeshraj_A_Resume.pdf`.
4. Replace the `COLLEGE LOGO` placeholders with actual logo images if desired.
5. Replace the two `LOGO` placeholders in Experience with the Cognizant logo.

## GitHub Pages

Create a repository, for example `ganeshraj-a.github.io` if you want a user-site URL.

Then:

```bash
git init
git add .
git commit -m "Create AI engineer portfolio"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPOSITORY.git
git push -u origin main
```

On GitHub:
Settings → Pages → Build and deployment → Source: Deploy from a branch → Branch: `main` → Folder: `/ (root)` → Save.

Your site will then be available at the GitHub Pages URL shown by GitHub.
